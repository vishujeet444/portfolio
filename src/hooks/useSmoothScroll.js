/**
 * Native scroll only — Lenis removed for performance.
 * Smooth scrolling handled via CSS `scroll-behavior: smooth` on html.
 */
export default function useSmoothScroll() {
  // Intentionally empty — avoids a perpetual requestAnimationFrame loop.
}
